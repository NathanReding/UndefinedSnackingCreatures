namespace DTOs{
    // used in the board to be capsul for all the data that might be on a tile
    public class PositionObject{
        string tileEffect = "";
        CreatureObject creature;
        // StructureObject structure;
        // ?? wall??
    }
}